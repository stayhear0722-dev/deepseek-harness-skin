# DeepSeek Harness 定制皮肤

这是可独立运行的 DeepSeek Harness 源码版本，包含深海蓝玻璃质感界面、可交互的粒子背景，以及由粒子组成的 DeepSeek 官方鲸鱼标志。

## 在另一台 Windows 电脑安装

1. 从此 GitHub 私有仓库下载或克隆代码。
2. 安装 Node.js 22 或以上版本（建议使用官网 LTS 版本）。
3. 在项目目录中打开 PowerShell，执行：

   ```powershell
   corepack enable
   corepack pnpm install --frozen-lockfile
   corepack pnpm run build:lib
   corepack pnpm --filter @deepseek-ai/dsh-web-frontend run build
   ```

4. 双击 `启动定制版.cmd`。
5. 在浏览器打开终端显示的本地地址（通常是 `http://127.0.0.1:3080`）。

## 数据与升级

- `data`：本机运行数据；不会随 GitHub 上传或在两台电脑间自动同步。
- `workspace`：本机工作区文件；同样不上传。若要两台电脑共享文件，建议把工作区改为 OneDrive、企业网盘或 Git 仓库中的独立目录。
- 更新皮肤版本时，在项目目录执行 `git pull`，然后再次执行上面的两条构建命令。

## 安全提示

请勿把 `.env`、账号令牌、`data` 或 `workspace` 中的私人资料提交到 GitHub。
